export const DEFAULT_WORKSPACE_NAME = "Poza Nutą";
export const DEFAULT_WORKSPACE_HANDLE = "pozanuta";

export function isDefaultWorkspaceHandle(handle: string) {
  return handle === DEFAULT_WORKSPACE_HANDLE;
}
